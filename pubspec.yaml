name: mybillbox
description: "A new Billing software"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.1.0
  intl: ^0.18.0
  cupertino_icons: ^1.0.8
  animations: ^2.1.2
  animated_snack_bar: ^0.4.0
  shared_preferences: ^2.5.5
  get: ^4.7.3
  provider: ^6.1.5+1
  image_picker: ^1.2.1
  cached_network_image: ^3.4.1
  pdf: ^3.11.0
  printing: ^5.13.1
  share_plus: ^10.0.0
  path_provider: ^2.1.0
  url_launcher: ^6.3.0
  fl_chart:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/